# Discord application (discord.com/developers/applications)
DISCORD_TOKEN=
DISCORD_APP_ID=
# optional: register commands to one server instantly instead of globally
GUILD_ID=
# encrypts player keys at rest (any long random string); falls back to PRIVATE_KEY
BOT_SECRET=
# the operator key from ../.env is used to top up player gas
